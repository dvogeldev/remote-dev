# Open Brain is an MCP resource; the dock is files

Capture this phase is `~/agency/inbox/` files. Drain/promote those to canon. Open Brain (Supabase `crgdufvwyfgbzobtnwcw`, ingest at `…/functions/v1/ingest-thought`) is already running; Hermes on `default` **reads** it via MCP when a wiki page or method needs context. It is not the loading dock, not `hermes memory setup`, not Cognee.

Telegram paste is how capture often happens today; it becomes a pipe into the file inbox (or a thought) when gateway exists — not this phase’s dock.

Inbox-file → `ingest-thought` is a **later, selective** skill, not part of first drain. Rejected: Open Brain as CMS/tasks/graph; draining OB as the inbox; auto-embedding the file hose into pgvector on day one.
