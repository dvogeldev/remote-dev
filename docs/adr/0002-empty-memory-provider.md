# Hermes memory provider slot stays empty

Hermes keeps built-in MEMORY.md / USER.md / session FTS and does not occupy the external MemoryProvider slot (`hermes memory off`). Canon is a git wiki; Cognee may later catalog promoted pages as tools, never as the provider with session-end promotion into the graph. Rejected: playbook §7 (`memory.provider: cognee`, `COGNEE_IMPROVE_ON_END`) and map #1's earlier note that Hermes uses Cognee for memory. One extractor per stream; filling the slot "to be safe" is how you get two truths.
